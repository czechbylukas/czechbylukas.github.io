runtime: python310
instance_class: F1

# This is the "Key": it tells Google to look in the subfolder for the app
entrypoint: gunicorn -b :$PORT lessons.python.app:app

automatic_scaling:
  max_instances: 1
  min_instances: 0