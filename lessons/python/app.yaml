runtime: python310
instance_class: F1

# Change this line! We remove "lessons.python" because inside the cloud, 
# app.py is sitting right in the main directory.
entrypoint: gunicorn -b :$PORT app:app

automatic_scaling:
  max_instances: 1
  min_instances: 0